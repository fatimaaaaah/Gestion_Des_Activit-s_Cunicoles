import 'package:flutter/material.dart';

class PurchaseJournalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Journal des achats'),
      ),
      body: Center(
        child: Text('Contenu du journal des achats'),
      ),
    );
  }
}
